    <UserControl x:Class="super_rookie.UserControls.Settings.LevelSensorSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15">
            <TextBlock Text="Level Sensor Settings" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50" 
                       Margin="0,0,0,15"/>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Trigger Amount:" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TriggerAmount}" Margin="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Status DI:" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox x:Name="StatusDiComboBox" Grid.Row="2" Grid.Column="1" SelectedItem="{Binding StatusDi}" Margin="5">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Tank:" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox x:Name="TankComboBox" Grid.Row="3" Grid.Column="1" SelectedItem="{Binding Tank}" Margin="5">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
