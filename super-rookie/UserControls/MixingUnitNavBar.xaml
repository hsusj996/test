<UserControl x:Class="super_rookie.UserControls.MixingUnitNavBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:super_rookie.Converters"
             Height="50">

    <UserControl.Resources>
        <converters:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
    </UserControl.Resources>

    <Border Background="#2C3E50" Padding="10,5">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Mixing Units:" 
                       Foreground="White" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"
                       FontWeight="Bold"/>
            <ItemsControl ItemsSource="{Binding MixingUnits}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}" 
                                Background="{Binding IsSelected, Converter={StaticResource BooleanToBrushConverter}}" 
                                Foreground="White" 
                                Padding="15,8" 
                                Margin="2" 
                                BorderThickness="0"
                                FontWeight="Bold"
                                Command="{Binding DataContext.SelectMixingUnitCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>