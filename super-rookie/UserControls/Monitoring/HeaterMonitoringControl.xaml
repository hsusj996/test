    <UserControl x:Class="super_rookie.UserControls.Monitoring.HeaterMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FFE8E8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Heater 영역 헤더 -->
        <Border Grid.Row="0" 
                Background="#E74C3C" 
                Padding="10,8">
            <TextBlock Text="Heater" 
                       FontSize="14" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Heater 영역 콘텐츠 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Auto"
                      Margin="10">
            <ItemsControl ItemsSource="{Binding Heaters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 개별 Heater 영역 -->
                        <Border Background="White" 
                                BorderBrush="#E74C3C" 
                                BorderThickness="2" 
                                CornerRadius="5" 
                                Margin="5" 
                                Padding="10"
                                Width="150"
                                VerticalAlignment="Stretch">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Heater 제목 -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding Name}" 
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           Foreground="#E74C3C" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,10"/>
                                
                                <!-- Heater 상태 -->
                                <Border Grid.Row="1" 
                                        Background="#FFEBEE" 
                                        BorderBrush="#E74C3C" 
                                        BorderThickness="1" 
                                        CornerRadius="3" 
                                        Margin="5">
                                    <StackPanel HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Ellipse Width="20" 
                                                 Height="20" 
                                                 Fill="#E74C3C" 
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="Heater" 
                                                   FontSize="10" 
                                                   Foreground="#E74C3C"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
