    <UserControl x:Class="super_rookie.UserControls.Monitoring.MixerMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F0F8E8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Mixer 영역 헤더 -->
        <Border Grid.Row="0" 
                Background="#27AE60" 
                Padding="10,8">
            <TextBlock Text="Mixer" 
                       FontSize="14" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Mixer 영역 콘텐츠 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Auto"
                      Margin="10">
            <ItemsControl ItemsSource="{Binding Mixers}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 개별 Mixer 영역 -->
                        <Border Background="White" 
                                BorderBrush="#27AE60" 
                                BorderThickness="2" 
                                CornerRadius="5" 
                                Margin="5" 
                                Padding="10"
                                Width="150"
                                VerticalAlignment="Stretch">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Mixer 제목 -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding Name}" 
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           Foreground="#27AE60" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,10"/>
                                
                                <!-- Mixer 상태 -->
                                <Border Grid.Row="1" 
                                        Background="#E8F5E8" 
                                        BorderBrush="#27AE60" 
                                        BorderThickness="1" 
                                        CornerRadius="3" 
                                        Margin="5">
                                    <StackPanel HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Ellipse Width="20" 
                                                 Height="20" 
                                                 Fill="#27AE60" 
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="Mixer" 
                                                   FontSize="10" 
                                                   Foreground="#27AE60"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
