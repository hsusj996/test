    <UserControl x:Class="super_rookie.UserControls.Monitoring.PumpMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#E8F4FD">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Pump 영역 헤더 -->
        <Border Grid.Row="0" 
                Background="#3498DB" 
                Padding="10,8">
            <TextBlock Text="Pump" 
                       FontSize="14" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Pump 영역 콘텐츠 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Auto"
                      Margin="10">
            <ItemsControl ItemsSource="{Binding Pumps}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 개별 Pump 영역 -->
                        <Border Background="White" 
                                BorderBrush="#3498DB" 
                                BorderThickness="2" 
                                CornerRadius="5" 
                                Margin="5" 
                                Padding="10"
                                Width="150"
                                VerticalAlignment="Stretch">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Pump 제목 -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding Name}" 
                                           FontSize="12" 
                                           FontWeight="Bold" 
                                           Foreground="#3498DB" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,10"/>
                                
                                <!-- Pump 상태 -->
                                <Border Grid.Row="1" 
                                        Background="#E3F2FD" 
                                        BorderBrush="#3498DB" 
                                        BorderThickness="1" 
                                        CornerRadius="3" 
                                        Margin="5">
                                    <StackPanel HorizontalAlignment="Center" 
                                                VerticalAlignment="Center">
                                        <Ellipse Width="20" 
                                                 Height="20" 
                                                 Fill="#3498DB" 
                                                 Margin="0,0,0,5"/>
                                        <TextBlock Text="Pump" 
                                                   FontSize="10" 
                                                   Foreground="#3498DB"
                                                   HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
