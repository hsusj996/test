    <UserControl x:Class="super_rookie.UserControls.Monitoring.ValveMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FDF2E9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Valve 영역 헤더 -->
        <Border Grid.Row="0" 
                Background="#E67E22" 
                Padding="15,10">
            <TextBlock Text="Valve Modules" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- 밸브 개수만큼 영역을 나누는 콘텐츠 -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto" 
                      HorizontalScrollBarVisibility="Auto"
                      Margin="10">
            <ItemsControl ItemsSource="{Binding Valves}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- 개별 밸브 영역 (표 형태) -->
                        <Border Background="White" 
                                BorderBrush="#BDC3C7" 
                                BorderThickness="1" 
                                CornerRadius="5" 
                                Margin="5" 
                                Padding="15"
                                Width="300"
                                VerticalAlignment="Stretch">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 밸브 제목 -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding Name}" 
                                           FontSize="16" 
                                           FontWeight="Bold" 
                                           Foreground="#2C3E50" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,0,0,15"/>
                                
                                <!-- 개폐 상태 -->
                                <Grid Grid.Row="1" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="상태:" 
                                               FontSize="12" 
                                               FontWeight="Bold" 
                                               Foreground="#7F8C8D" 
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding IsOpen, StringFormat={}{0:열림/닫힘}}" 
                                               FontSize="12" 
                                               Foreground="#2C3E50" 
                                               VerticalAlignment="Center"/>
                                </Grid>
                                
                                <!-- 유량 정보 -->
                                <Grid Grid.Row="2" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="유량:" 
                                               FontSize="12" 
                                               FontWeight="Bold" 
                                               Foreground="#7F8C8D" 
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding FlowRate, StringFormat={}{0:F1}L/min}" 
                                               FontSize="12" 
                                               Foreground="#2C3E50" 
                                               VerticalAlignment="Center"/>
                                </Grid>
                                
                                <!-- 방향 정보 -->
                                <Grid Grid.Row="3" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" 
                                               Text="방향:" 
                                               FontSize="12" 
                                               FontWeight="Bold" 
                                               Foreground="#7F8C8D" 
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Direction}" 
                                               FontSize="12" 
                                               Foreground="#E67E22" 
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                
                                <!-- 상태 표시 -->
                                <Grid Grid.Row="4">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 상태 헤더 -->
                                    <TextBlock Grid.Row="0" 
                                               Text="상태 표시:" 
                                               FontSize="11" 
                                               FontWeight="Bold" 
                                               Foreground="#7F8C8D" 
                                               Margin="0,0,0,5"/>
                                    
                                    <!-- 상태 표시 -->
                                    <Border Grid.Row="1" 
                                            Background="{Binding IsOpen, Converter={StaticResource BooleanToBrushConverter}}" 
                                            BorderBrush="#E67E22" 
                                            BorderThickness="1" 
                                            CornerRadius="3" 
                                            Margin="2" 
                                            Padding="5,2">
                                        <TextBlock Text="{Binding IsOpen, StringFormat={}{0:ON/OFF}}" 
                                                   FontSize="9" 
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
