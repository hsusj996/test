<UserControl x:Class="super_rookie.UserControls.HeaterGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <GroupBox Header="Heaters" 
              FontWeight="Bold" 
              BorderBrush="#BDC3C7">
        <DataGrid x:Name="DataGrid"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="None"
                  Background="Transparent"
                  RowHeight="80"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!-- 카드 형태의 행 -->
                            <Border Background="White" 
                                    BorderBrush="#E0E0E0" 
                                    BorderThickness="1" 
                                    CornerRadius="6" 
                                    Margin="5,2" 
                                    Padding="15,10">
                                <Border.Effect>
                                    <DropShadowEffect Color="Gray" 
                                                    Direction="270" 
                                                    ShadowDepth="1" 
                                                    Opacity="0.2" 
                                                    BlurRadius="3"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- 모듈 아이콘/상태 -->
                                    <Border Grid.Column="0" 
                                            Background="#E74C3C" 
                                            CornerRadius="4" 
                                            Padding="8,4" 
                                            Margin="0,0,15,0"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="HEATER" 
                                                   FontSize="10" 
                                                   FontWeight="Bold" 
                                                   Foreground="White" 
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                    
                                    <!-- 주요 정보 -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontSize="16" 
                                                   FontWeight="Bold" 
                                                   Foreground="#2C3E50" 
                                                   Margin="0,0,0,5"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding ControlOutput.Name, StringFormat='Control: {0}'}" 
                                                       FontSize="12" 
                                                       Foreground="#7F8C8D"/>
                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding StatusInput.Name, StringFormat='Status: {0}'}" 
                                                       FontSize="12" 
                                                       Foreground="#7F8C8D"/>
                                        </Grid>
                                    </StackPanel>
                                    
                                    <!-- 상태 표시 -->
                                    <Border Grid.Column="2" 
                                            Background="#E74C3C" 
                                            CornerRadius="4" 
                                            Padding="12,6"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="Active" 
                                                   FontSize="11" 
                                                   FontWeight="Bold" 
                                                   Foreground="White" 
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </GroupBox>
</UserControl>